concat aur push m difference ye h concat jo h add kr ke ek arrange m dusra new array deta h aur push ek m dusra add kr deta bus 


slice()
👉 Array ka ek hissa copy karta hai
👉 Original array change nahi hota
Syntax:

array.slice(start, end)
Example:

let arr = [1, 2, 3, 4, 5];

let result = arr.slice(1, 4);

console.log(result); // [2, 3, 4]
console.log(arr);    // [1, 2, 3, 4, 5] (same)
📌 end index include nahi hota
🔹 splice()
👉 Array ko modify karta hai
👉 Elements delete / add / replace sab kar sakta hai
Syntax:

array.splice(start, deleteCount, item1, item2, ...)
Example (remove):

let arr = [1, 2, 3, 4, 5];

arr.splice(1, 2);

console.log(arr); // [1, 4, 5]
Example (add):
let arr = [1, 2, 5];

arr.splice(2, 0, 3, 4);

console.log(arr); // [1, 2, 3, 4, 5]


1️⃣ Array.isArray()
👉 Check karta hai ye array hai ya nahi

Array.isArray(value)
Example:

Array.isArray([1,2,3]);   // true
Array.isArray("hello");  // false
Array.isArray({});       // false
📌 typeof array ke liye "object" deta hai, isliye array check ke liye hamesha Array.isArray() use karo
🔹 2️⃣ Array.of()
👉 Values se new array banata hai

Array.of(1, 2, 3);
Example:

Array.of(5);      // [5]
Array.of(1,2,3);  // [1,2,3]
📌 Difference:

Array(5);    // [empty × 5]
Array.of(5); // [5]
🧠 Array.of = jo diya wahi array me
🔹 3️⃣ Array.from()
👉 Array-like ya iterable ko array me convert karta hai

Array.from(value)
Example (string → array):

Array.from("hello");
// ['h','e','l','l','o']
Example (Set → array):

let set = new Set([1,2,3]);
Array.from(set); // [1,2,3]
Example (map ke sath):

Array.from([1,2,3], x => x * 2);
// [2,4,6]
🧠 from = convert karna
🔹 4️⃣ join()
👉 Array ko string me convert karta hai

array.join(separator)
Example:

let arr = ["HTML","CSS","JS"];

arr.join();      // "HTML,CSS,JS"
arr.join(" ");   // "HTML CSS JS"
arr.join("-");   // "HTML-CSS-JS"
📌 Original array change nahi hota
🔹 5️⃣ Spread Operator ...
👉 Array ke elements ko spread (phaila) deta hai
Example (copy):

let a = [1,2,3];
let b = [...a];

console.log(b); // [1,2,3]
Example (merge):

let a = [1,2];
let b = [3,4];

let c = [...a, ...b];
console.log(c); // [1,2,3,4]
Example (add element):

let arr = [2,3];
let newArr = [1, ...arr, 4];
// [1,2,3,4]
🧠 spread = copy / merge
🔹 6️⃣ flat()
👉 Nested array ko single level banata hai

array.flat(depth)
Example:

let arr = [1, [2, 3], [4, 5]];

arr.flat();
// [1,2,3,4,5]
Example (deep array):

let arr = [1,[2,[3,[4]]]];

arr.flat(2); // [1,2,3,[4]]
arr.flat(Infinity); // [1,2,3,4]
📌 Original array change nahi hota

destructuring of object ---

const user = {
  name: "Yaser",
  age: 22
};

const { name: username } = user;

console.log(username); // Yaser

Multiple Destructuring ek sath
const { courseName, price } = course;

Default Value in Destructuring
const { rating = 5 } = course;
console.log(rating); // 5